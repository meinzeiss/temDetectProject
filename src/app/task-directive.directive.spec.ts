import { TaskDirectiveDirective } from './task-directive.directive';

describe('TaskDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TaskDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
